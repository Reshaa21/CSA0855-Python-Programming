import math

def countVowelStrings(n):
    # Calculate the combination C(n + 4, 4)
    return math.comb(n + 4, 4)

# Test Case
n = 3
print(countVowelStrings(n))  # Output: 35
import math

def countVowelStrings(n):
    # Calculate the combination C(n + 4, 4)
    return math.comb(n + 4, 4)

# Test Case
n = 3
print(countVowelStrings(n))  # Output: 35
